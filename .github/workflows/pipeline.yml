name: SonarCloud Analysis

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
  workflow_dispatch: # Permite ejecución manual desde GitHub Actions UI

jobs:
  sonarcloud:
    name: Build and SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout del código
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Necesario para SonarCloud (análisis de historial completo)

      # 2. Configurar Node.js 20
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: my-app/package-lock.json

      # 3. Instalar dependencias
      - name: Install Dependencies
        working-directory: my-app
        run: npm ci --legacy-peer-deps

      # 4. Build de producción
      - name: Build Angular App
        working-directory: my-app
        run: npm run build -- --configuration production

      # 5. Análisis con SonarCloud
      - name: Build and Analyze with SonarCloud
        uses: sonarsource/sonarcloud-github-action@v3.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.qualitygate.wait=false

      # 6. Pipeline finalizado
      - name: Pipeline Finished
        run: echo "SonarCloud analysis completed successfully"
